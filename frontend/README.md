# Frontend (Angular) - Todo App

Instrucciones rápidas:

```bash
# Instalar dependencias
cd frontend
npm install

# Ejecutar servidor de desarrollo
npm start
```

El frontend usa Angular Material para UI y se comunica con el backend en `http://localhost:8080/api/tasks`.
