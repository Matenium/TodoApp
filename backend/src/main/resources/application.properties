# Puerto del backend
server.port=8080

# H2 (por defecto, memoria)
spring.datasource.url=jdbc:h2:mem:todo-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# H2 Console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Actuator
management.endpoints.web.exposure.include=health,info,metrics

# Logging
logging.level.org.springframework=INFO
logging.level.com.example=DEBUG

# --- Para usar SQLite en lugar de H2, reemplaza las líneas de datasource por:
# spring.datasource.url=jdbc:sqlite:todo.db
# spring.datasource.driverClassName=org.sqlite.JDBC
# spring.jpa.database-platform=com.example.todo.config.SQLiteDialect
# spring.jpa.hibernate.ddl-auto=update
